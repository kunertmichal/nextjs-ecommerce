const title = "Kunert Shop";
const description = "Ecommerce for learning purpose";

const config = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: title,
  },
};

export default config;
